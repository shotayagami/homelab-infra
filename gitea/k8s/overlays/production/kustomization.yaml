apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitea

resources:
  - ../../base

# Secrets (gitea-db, gitea-redis) are managed out-of-band with kubectl create.
# Migrating them to SealedSecret is tracked as future work in the parent README.
